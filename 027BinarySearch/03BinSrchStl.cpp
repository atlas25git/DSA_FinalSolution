#include<algorithm>
using namespace std;

int main(){
    
    //binary_search(arr,arr+n,key) ->returns true or false;
    //Finding index:
    //->auto it =lower_bound(arr,arr+n,key) -> index = it-arr;
    //ub could be used to give next bigger element greater than the no.
}