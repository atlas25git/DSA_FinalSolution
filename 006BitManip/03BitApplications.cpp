//check odd
// no & 1 ? "odd":"even";

//finding i'th bit from right
//creating mask and &
//create mask and shift 1 left for given ith value
//no & mask ? set: not Set

//set ith bit of given no.
//or of created mask with the no.

//clear ith bit
//create a mask with ith bit zero and all other 1
//negate the mask created in the previous method
//no= no & ~(i<<i)

//clear a range of bits
//-1 is all 1s 
//left shift to introduce 0s in sarting i positions
// n & (-1<<i);

