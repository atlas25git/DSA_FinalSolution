//used for range based queries:
//they could be : minRange, MaxRange, subarray sum etc;
//brute force 0(n)xQ
//we can do pre computation in O(N^2) but that too won't work for very large nos.
//and even update becomes diffcult under the given timestate

//update too is slow for table based approach
